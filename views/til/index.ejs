<% include ../head %>

  <ul class="list-unstyled">
    <% for(var i=0; i<til.length; i++) { %>
      <li>
        <a href='/til/<%= entries[i].id %>'><%= til[i].slug %></a>
        <a href='/til/<%= entries[i].id %>/edit'>Edit</a>
        <a href='/til/<%= entries[i].id %>/delete'>Delete</a>
      </li>
    <% } %>
  </ul>

<% include ../footer %>
